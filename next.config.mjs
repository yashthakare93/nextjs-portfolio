/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['github-profile-trophy.vercel.app', 'api.microlink.io', 'i.pinimg.com'],
      },
};

export default nextConfig;
